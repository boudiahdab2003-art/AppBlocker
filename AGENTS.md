# AppBlocker — agent instructions

See **[CLAUDE.md](CLAUDE.md)**. It is the single source of truth for this project's
workflow, release process and device quirks, and it applies to every coding agent
(Codex, Claude Code, anything else) working in this repo.

This file used to be a byte-identical copy of it, which only meant the two could drift
apart. Update CLAUDE.md instead.
