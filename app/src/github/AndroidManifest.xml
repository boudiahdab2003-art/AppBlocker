<?xml version="1.0" encoding="utf-8"?>
<!-- GitHub (sideloaded) flavor only. The manifest merger unions this with src/main's manifest.
     None of this may ship in the Play flavor: self-updating APKs are forbidden on Play, and the
     location permissions would trigger Google's heaviest review. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- In-app updater: install the downloaded release APK. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Wi-Fi & Location schedules. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application>
        <!-- Serves the downloaded update APK to the system installer. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
